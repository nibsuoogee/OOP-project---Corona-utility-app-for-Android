<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Olio_HT" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bars_night</item>
        <item name="colorPrimaryVariant">@color/bars_night</item>
        <item name="colorOnPrimary">@color/background_night</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/boxes_night</item>
        <item name="colorSecondaryVariant">@color/text_night</item>
        <item name="colorOnSecondary">@color/accent_night</item>
        <item name="colorSecondaryContainer">@color/chart_background_night</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">?attr/colorOnPrimary</item>
        <item name="android:textColor">@color/text_night</item>
    </style>
</resources>